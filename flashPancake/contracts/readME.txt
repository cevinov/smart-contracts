The interface directory maintains a list of all ABIs (Functionality) required to interact with the smart contract

The library directory contains program source code required for other scripts/code (if needed, we can import it)